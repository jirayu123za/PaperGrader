services:
  frontend:
    image: papergrader-frontend:prod
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: "http://localhost:5173"
        NEXT_PUBLIC_API_URL: "/api"
        BACKEND_ORIGIN: "http://host.docker.internal:3000/api"
    ports:
      - "5173:3000"